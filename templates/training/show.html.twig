{% extends 'base.html.twig' %}

{% block title %}Training{% endblock %}

{% block body %}
    <h1>Training</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ training.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ training.Name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ training.description }}</td>
            </tr>
            <tr>
                <th>Duration</th>
                <td>{{ training.duration }}</td>
            </tr>
            <tr>
                <th>Extra_cost</th>
                <td>{{ training.extraCost }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ training.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admintraining') }}">back to list</a>

    <a href="{{ path('app_training_edit', {'id': training.id}) }}">edit</a>

    {{ include('training/_delete_form.html.twig') }}
{% endblock %}
